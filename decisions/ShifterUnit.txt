country_decisions = {
	shifter_seek_new_form = {
		major = yes 
		potential = {
			has_estate_privilege = estate_nobility_patrons_of_the_lodges
		}

		provinces_to_highlight = {
			has_province_modifier = shifter_lodge_active
		}

		allow = {
			hidden_trigger = { has_estate_privilege = estate_nobility_patrons_of_the_lodges }
			army_tradition = 10 
			custom_trigger_tooltip = {
				tooltip = shifter_seek_new_form_trigger_tooltip
				NOT = {
					has_country_modifier = shifter_change_substype_dummy_cooldown
					has_country_flag = shifter_no_open_descion_while_event_open
				}
			}
			num_of_owned_provinces_with = {
				has_province_modifier = shifter_lodge_active
				value = 3 
			}
		}

		effect = {
			country_event = {
				id = sarhalevents.402 
			}
		}
		
	}

}