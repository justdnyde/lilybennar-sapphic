
convert_medasi =  {
	category = influence
	require_acceptance = no
	is_visible = {
		is_emperor_of_china = yes
		overlord_of = FROM
		FROM = { is_subject_of_type = tributary_state }
	}
	
	is_allowed = {
		hidden_trigger = {
			overlord_of = FROM
			FROM = { is_subject_of_type = tributary_state }
		}
		if = { #Samsumbat
			limit = {
				is_or_was_tag = { tag = F60 }
			}
			empire_of_china_reform_passed = republicanism_steward_reform
		}
		else = {
			empire_of_china_reform_passed = establish_medasi_reform
		}
		imperial_mandate = 60
		is_at_war = no
		FROM = {
			primary_culture = ROOT
			religion = ROOT
			NOT = { liberty_desire = 50 }
		}
		if = {
			limit = { mission_completed = konolkhatep_expand_medasi }
			custom_trigger_tooltip = {
				tooltip = gnoll_much_medasi
				NOT = {
					check_variable = {
						which = num_medasi						
						which = num_china_reform
					}
				}
			}
			FROM = {
				total_development = 150
				NOT = { num_of_rebel_armies = 1 }
				NOT = { num_of_rebel_controlled_provinces = 1 }
			}
		}
	}
	
	on_accept = {
		if = {
			limit = {
				FROM = { total_development = 500 }
			}
			add_mandate = -30
		}
		else_if = {
			limit = {
				FROM = { total_development = 300 }
			}
			add_mandate = -20
		}
		else = { add_mandate = -10 }
		FROM = { grant_independence = yes }
		create_subject = {
			subject_type = medasi_subject
			subject = FROM
		}
		if = {
			limit = { mission_completed = konolkhatep_expand_medasi }
			FROM = { konolkhatep_new_gnoll_medasi = yes	}
		}
		if = { # Samsumbat
			limit = { mission_completed = F60_iron_trees_iron_chains }
			FROM = {
				anb_sarhal_cultural_union = {
					west_sarhaly = yes
				}
			}
		}
		if = { # Samsumbat
			limit = { mission_completed = F60_a_great_union_of_states }
			FROM = {
				add_country_modifier = {
					name = F60_great_union_of_states
					duration = -1
				}
			}
		}
	}
	
	ai_will_do = {
		stability = 1
		imperial_mandate = 80
	}
}