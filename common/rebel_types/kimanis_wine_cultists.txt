######################################################################
# Kimanis Wine Cultists
######################################################################

kimanis_wine_cultists = {

	color = { 102 0 204 } # purple

	area = nation 
	government = any
	defection = none			
	defect_delay = 120
	independence = none	
	unit_transfer = no
	gfx_type = culture_province
	will_relocate = yes	
	
	resilient = yes			
	reinforcing = yes
	general = yes
	smart = yes
	
	artillery = 0.3
	infantry = 0.6 
	cavalry = 0.1
	
	morale = 1.5

	# Possible handle actions
	handle_action_negotiate = yes			#yes/no
	handle_action_stability = yes			#yes/no
	handle_action_build_core = yes			#yes/no
	handle_action_send_missionary = yes			#yes/no	
	
	# The rebel type with the highest modifier for this province gets picked
	spawn_chance = {
		factor = 1
		modifier = {
			factor = 100
			has_province_modifier = G61_wine_cult
		}
		modifier = {
			factor = 0
			NOT = { has_province_modifier = G61_wine_cult }
		}
		modifier = {
			factor = 0
			culture = kimabhen
		}
	}

	# This is checked for EACH province in the Area of Operations
	movement_evaluation = {
		factor = 1
		modifier = {
			factor = 0.1
			units_in_province = 1
		}
		modifier = {
			factor = 0.001
			controlled_by = REB
		}
		modifier = {
			factor = 10
			is_capital = yes
		}
		modifier = {
			factor = 5
			has_province_modifier = G61_wine_cult
		}
	}

	# Province Scope
	siege_won_trigger = {
		# none
	}
	siege_won_effect = {
		# none
	}
	
	# Country scope
	can_negotiate_trigger = {
		always = yes
	}
	
	# Country scope
	can_enforce_trigger = {
		always = yes
		any_owned_province = {
			has_rebel_faction = kimanis_wine_cultists # unsure if this required?
		}
	}
	
	# Localisation for their demands
	demands_description = "kimanis_wine_cultists_demand" # TODO LVT all the loc for this
	
	# Country Scope	
	demands_enforced_effect = {
		if = {
			limit = { has_country_flag = G61_gambit_active } # if kimanis is doing the gambit
			country_event = { id = flavour_kimanis.19 } # gambit failed
		}
		else = {
			every_owned_province = {
				limit = {
					has_rebel_faction = kimanis_wine_cultists
					has_ruinborn_majority_trigger = yes # could maybe also be south ruinborn culture group?
				}
				change_culture = kimabhen
				add_nationalism = 20 # TODO test this, both functional and value, maybe way to low, cause rebel cooldown
			}
		}
	}
}
