vir_diligence = {
	discipline = 0.05

	sprite = 231

	potential = {
		religion = infinite_self
	}
	effect = {
		if = {
			limit = { NOT = { has_country_modifier = vir_diligence_mastered } }
			hidden_effect = { set_estate_privilege = estate_church_vir_diligence }
			custom_tooltip = vir_diligence_tt
		}
		else = {
			custom_tooltip = vir_stuck_tt
		}
	}
	ai_will_do = {
		factor = 1
	}
}

vir_cooperation = {
	trade_efficiency = 0.15

	sprite = 229
	potential = {
		religion = infinite_self
	}
	effect = {
		if = {
			limit = { NOT = { has_country_modifier = vir_cooperation_mastered } }
			hidden_effect = { set_estate_privilege = estate_church_vir_cooperation }
			custom_tooltip = vir_cooperation_tt
		}
		else = {
			custom_tooltip = vir_stuck_tt
		}
	}
	ai_will_do = {
		factor = 1
	}
}

vir_forbearance = {
	global_regiment_cost = -0.1

	sprite = 230

	potential = {
		religion = infinite_self
	}
	effect = {
		if = {
			limit = { NOT = { has_country_modifier = vir_forbearance_mastered } }
			hidden_effect = { set_estate_privilege = estate_church_vir_forbearance }
			custom_tooltip = vir_forbearance_tt
		}
		else = {
			custom_tooltip = vir_stuck_tt
		}
	}
	ai_will_do = {
		factor = 1
	}
}

vir_honesty = {
	yearly_corruption = -0.10

	sprite = 232

	potential = {
		religion = infinite_self
	}
	effect = {
		if = {
			limit = { NOT = { has_country_modifier = vir_honesty_mastered } }
			hidden_effect = { set_estate_privilege = estate_church_vir_honesty }
			custom_tooltip = vir_honesty_tt
		}
		else = {
			custom_tooltip = vir_stuck_tt
		}
	}
	ai_will_do = {
		factor = 1
	}
}

vir_awareness = {
	core_creation = -0.05

	sprite = 233

	potential = {
		religion = infinite_self
	}
	effect = {
		if = {
			limit = { NOT = { has_country_modifier = vir_awareness_mastered } }
			hidden_effect = { set_estate_privilege = estate_church_vir_awareness }
			custom_tooltip = vir_awareness_tt
		}
		else = {
			custom_tooltip = vir_stuck_tt
		}
	}
	ai_will_do = {
		factor = 1
	}
}

vir_acceptance = {
	global_missionary_strength = 0.02

	sprite = 236

	potential = {
		religion = infinite_self
		is_religion_enabled = molten_eternity
	}
	effect = {
		if = {
			limit = { NOT = { has_country_modifier = vir_acceptance_mastered } }
			hidden_effect = { set_estate_privilege = estate_church_vir_acceptance }
			custom_tooltip = vir_acceptance_tt
		}
		else = {
			custom_tooltip = vir_stuck_tt
		}
	}
	ai_will_do = {
		factor = 1
	}
}

vir_integration = { 
	global_autonomy = -0.05

	sprite = 234

	potential = { 
		religion = infinite_self
		has_global_flag = vir_integration_allowed
	}
	effect = {
		if = {
			limit = { NOT = { has_country_modifier = vir_integration_mastered } }
			hidden_effect = { set_estate_privilege = estate_church_vir_integration }
			custom_tooltip = vir_integration_tt
		}
		else = {
			custom_tooltip = vir_stuck_tt
		}
	}
	ai_will_do = {
		factor = 1
	}
}

vir_transcendance = { 
	land_morale = 0.10

	sprite = 235

	potential = { 
		religion = infinite_self
		has_global_flag = vir_transcendance_allowed
	}
	effect = {
		if = {
			limit = { NOT = { has_country_modifier = vir_transcendance_mastered } }
			hidden_effect = { set_estate_privilege = estate_church_vir_transcendance }
			custom_tooltip = vir_transcendance_tt
		}
		else = {
			custom_tooltip = vir_stuck_tt
		}
	}
	ai_will_do = {
		factor = 1
	}
}

vir_globalism = { 
	diplomatic_reputation = 2

	sprite = 237

	potential = {
		religion = infinite_self
		has_global_flag = vir_globalism_allowed
	}
	effect = {
		if = {
			limit = { 
				NOT = {
					OR = {
						has_country_modifier = vir_globalism_embraced
						has_country_modifier = vir_globalism_defeated
					}
				}
			}
			hidden_effect = { 
				country_event = {
					id = infinite_self.5
				}
				set_estate_privilege = estate_church_vir_globalism
			}
			custom_tooltip = vir_globalism_tt
		}
		else = {
			custom_tooltip = vir_stuck_tt
		}
	}
	ai_will_do = {
		factor = 1
	}
}