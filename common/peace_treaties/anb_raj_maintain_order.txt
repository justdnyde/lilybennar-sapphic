po_raj_maintain_order = {
	category = 6
	power_projection = no
	power_cost_base = 0.0 
	prestige_base = 0.3
	ae_base = 0
	warscore_cost = {
		all_provinces = 0.0
		no_provinces = 15.0
		owner_keeps = 0.0
		conquered = 0.0
		returned_core = 0.0
		released_nation = 0.0
		cancelled_subject = 0.0 
		concede_colonial_region = 0.0
	}
	warscore_cap = 15
	requires_demand_independence = no 
	is_make_subject = no
	requires_is_allowed = no
	
	is_visible = {
		is_subject_of_the_raja = yes
		FROM = { is_subject_of_the_raja = yes }
	}
	is_allowed = { 
		always = yes
	}
	effect = { 
		FROM = { add_liberty_desire = -25 }
		overlord = {
			add_opinion = {
				who = ROOT
				modifier = raj_pacified_another_subject
			}
		}
	}
	ai_weight = { 
		export_to_variable = {
			variable_name = ai_value 
			value = trigger_value:always
		}
		set_variable = {
			ai_value = 0
		}
	}
}