po_parusahan_saya_rein_in_vizier = {
	category = 6
	power_projection = no
	power_cost_base = 0.0 
	prestige_base = 0.3
	ae_base = 0
	warscore_cost = {
		all_provinces = 0.0
		no_provinces = 90.0
		owner_keeps = 0.0
		conquered = 0.0
		returned_core = 0.0
		released_nation = 0.0
		cancelled_subject = 0.0 
		concede_colonial_region = 0.0
	}
	warscore_cap = -1
	requires_demand_independence = no 
	is_make_subject = no
	requires_is_allowed = no
	
	is_visible = {
		FROM = {
			is_in_war = {
				defender_leader = ROOT
				attacker_leader = THIS
				casus_belli = cb_independence_war
			}
		}
		has_country_flag = R54_vizier_revolt
	}
	is_allowed = { 
		always = yes
	}
	effect = {
		country_event = {
			id = flavor_parusahan_saya.18
		}
	}
	ai_weight = { 
		export_to_variable = {
			variable_name = ai_value 
			value = trigger_value:always
		}
		set_variable = {
			ai_value = 0
		}
	}
}