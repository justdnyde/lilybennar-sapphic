po_parusahan_saya_dominate_haless = {
	category = 6
	power_projection = no
	power_cost_base = 0.0 
	prestige_base = 0.3
	ae_base = 0
	warscore_cost = {
		all_provinces = 0.0
		no_provinces = 80.0
		owner_keeps = 0.0
		conquered = 0.0
		returned_core = 0.0
		released_nation = 0.0
		cancelled_subject = 0.0 
		concede_colonial_region = 0.0
	}
	warscore_cap = -1
	requires_demand_independence = no 
	is_make_subject = no
	requires_is_allowed = yes
	
	is_visible = {
		FROM = { has_country_flag = R54_warleader_flag }
	}
	is_allowed = { 
		always = yes
	}
	effect = {
		hidden_effect = {
			set_country_flag = R54_won_great_war
			FROM = { clr_country_flag = R54_warleader_flag }
		}
	}
	ai_weight = { 
		export_to_variable = {
			variable_name = ai_value 
			value = trigger_value:always
		}
		set_variable = {
			ai_value = 0
		}
	}
}